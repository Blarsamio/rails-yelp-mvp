<div class="container">
  <% @restaurants.each do |r| %>
    <h3>
      <%= link_to r.name, restaurant_path(r)  %>
    </h3>
    <h4><%= r.address %></h4>
    <h4><%= r.category %></h4>
    <h4><%= r.phone_number %></h4>
  <% end %>
</div>
